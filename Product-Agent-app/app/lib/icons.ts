import {
  Target, TrendingUp, Lightbulb, Puzzle, HelpCircle, FlaskConical, LayoutGrid,
  type LucideIcon,
} from "lucide-react";

/** Shared icon lookup — maps icon name strings (from LEVEL_META.icon) to Lucide components */
export const LEVEL_ICON_MAP: Record<string, LucideIcon> = {
  Target, TrendingUp, Lightbulb, Puzzle, HelpCircle, FlaskConical, LayoutGrid,
};
